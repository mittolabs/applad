import 'package:flutter/material.dart';

class DeployPage extends StatelessWidget {
  const DeployPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Deploy')),
      body: const Center(child: Text('Deploy')),
    );
  }
}
