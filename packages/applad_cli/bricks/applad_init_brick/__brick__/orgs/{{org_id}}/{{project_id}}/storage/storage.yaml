# ============================================================
# STORAGE ADAPTER CONFIGURATION
# Adapter config here. Bucket definitions in separate files.
# File metadata in runtime database. Credentials in admin db.
# Per-environment overrides for local/VPS/cloud continuum.
# ============================================================

adapter: "local" # local | s3 | gcs | r2 | sftp | custom

#config:
#  bucket: ${S3_BUCKET}
#  region: ${S3_REGION}
#  access_key: ${S3_ACCESS_KEY}
#  secret_key: ${S3_SECRET_KEY}
#  endpoint: ${S3_ENDPOINT}           # For R2 or custom S3-compatible endpoints
#  ssl: true

environment_overrides:
  development:
    adapter: "local"
    config:
      path: "./data/storage" # Local filesystem in dev — no cloud cost


#  staging:
#    adapter: "s3"
#    config:
#      bucket: ${STAGING_S3_BUCKET}
#      region: ${S3_REGION}
#      access_key: ${S3_ACCESS_KEY}
#      secret_key: ${S3_SECRET_KEY}
