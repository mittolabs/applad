# Applad Core

Shared models, configuration parsing logic, and the centralized YAML merge engine for the Applad architecture platform.

This package provides the underlying strictly-typed structural models consumed by `applad_server` and `applad_cli`.

For complete documentation visit the [Applad GitHub Repository](https://github.com/mittolabs/applad).
